const generatedFourRandomWords = (wordData) => {
  // should return an array of four random objects in the wordData array

  // If you're not sure, think about how you get one random entry from the wordData
  // array first.
  // Then how would you put that random entry into a new array.
};
